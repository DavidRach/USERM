% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredIsobandPlotMulti.R
\name{PredIsobandPlotMulti}
\alias{PredIsobandPlotMulti}
\title{Overlay Prediction Grids Using Isobands or Contour Lines}
\usage{
PredIsobandPlotMulti(
  grid_list,
  x_label = "X",
  y_label = "Y",
  x_scale = "Linear",
  y_scale = "Linear",
  x_cofactor = 5,
  y_cofactor = 5,
  x_min = NULL,
  x_max = NULL,
  y_min = NULL,
  y_max = NULL,
  mode = "Pseudo-color",
  legend_show = TRUE,
  label_population = NA,
  intensity_matrix = NA
)
}
\arguments{
\item{grid_list}{A named list of data frames, each containing columns \code{x}, \code{y}, and \code{z}.
Each data frame represents a prediction grid for a model.}

\item{x_label}{A character string for the x-axis label.}

\item{y_label}{A character string for the y-axis label.}

\item{x_scale}{A character string specifying the x-axis scale. Must be one of \code{"Linear"}, \code{"Log10"}, or \code{"Arcsinh"}.}

\item{y_scale}{A character string specifying the y-axis scale. Must be one of \code{"Linear"}, \code{"Log10"}, or \code{"Arcsinh"}.}

\item{x_cofactor}{A numeric value used for Arcsinh transformation on the x-axis. Ignored for other scales.}

\item{y_cofactor}{A numeric value used for Arcsinh transformation on the y-axis. Ignored for other scales.}

\item{x_min}{Minimum value for the x-axis.}

\item{x_max}{Maximum value for the x-axis.}

\item{y_min}{Minimum value for the y-axis.}

\item{y_max}{Maximum value for the y-axis.}

\item{mode}{A character string specifying the plot mode. Must be either \code{"Contour line"} or \code{"Pseudo-color"}.}

\item{legend_show}{Logical; whether to show the legend. Default is \code{TRUE}.}

\item{label_population}{Optional character vector specifying population names to label on the plot.}

\item{intensity_matrix}{Optional matrix or data frame containing intensity values for labeling populations.
Must include rows named by \code{x_label} and \code{y_label}.}
}
\value{
A \code{ggplot} object representing the combined prediction plot.
}
\description{
This function creates a ggplot-based visualization that overlays multiple prediction grids
on a single plot. Each grid represents a model's prediction and is distinguished by color.
The function supports various axis scaling methods (Linear, Log10, Arcsinh), and optionally
overlays population labels based on an intensity matrix.
}
\examples{
\dontrun{
PredIsobandPlotMulti(grid_list,
                     x_label = "X", y_label = "Y",
                     x_scale = "Linear", y_scale = "Linear",
                     x_min = 0, x_max = 1000,
                     y_min = 0, y_max = 1000,
                     mode = "Pseudo-color")
}
}
