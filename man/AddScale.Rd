% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AddScale.R
\name{AddScale}
\alias{AddScale}
\title{Add scaling parameters for a specific fluorophore}
\usage{
AddScale(Userm, fluor, min = -100, max = 1000, scale = "Linear", cofactor = 10)
}
\arguments{
\item{Userm}{A userm object created by \code{\link{CreateUserm}}, containing fluor and detector information.}

\item{fluor}{A character string specifying the fluorophore to update.}

\item{min}{A numeric value specifying the minimum intensity threshold. Default is -100.}

\item{max}{A numeric value specifying the maximum intensity threshold. Default is 1000.}

\item{scale}{A character string specifying the scaling method. Must be one of \code{"Linear"}, \code{"Log10"}, or \code{"Arcsinh"}. Default is \code{"Linear"}.}

\item{cofactor}{A positive numeric value used for scaling (only used for \code{"Arcsinh"}). Default is 10.}
}
\value{
The updated \code{Userm} object with new scaling parameters for the specified fluorophore.
}
\description{
This function adds or updates scaling parameters for a given fluorophore in a userm object.
It validates the input fluor name, scaling method, and numeric parameters, then stores them in the
\code{Scale_df} field of the \code{Userm} object.
}
\examples{
\dontrun{
A <- matrix(runif(20), nrow = 4, ncol = 5)
rownames(A) <- paste0("D", 1:4)
colnames(A) <- paste0("F", 1:5)
Userm <- CreateUserm(A)
Userm <- AddScale(Userm, fluor = "F1", min = 0, max = 5000, scale = "Arcsinh", cofactor = 5)
}
}
