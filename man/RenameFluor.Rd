% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RenameFluor.R
\name{RenameFluor}
\alias{RenameFluor}
\title{Rename a fluorophore in a Userm object}
\usage{
RenameFluor(Userm, raw_name, new_name)
}
\arguments{
\item{Userm}{A \code{Userm} object containing fluorophore information, matrices,
and results. Must include elements \code{fluors}, \code{A}, \code{Scale_df},
\code{Intensity_mtx}, \code{Res}, and \code{Rename_table}.}

\item{raw_name}{A character string specifying the existing fluorophore name
to be replaced. Must exist in \code{Userm$fluors} or \code{Userm$Rename_table}.}

\item{new_name}{A character string specifying the new fluorophore name.
Must not already exist in \code{Userm$fluors}.}
}
\value{
The updated \code{Userm} object with the fluorophore renamed consistently
across all relevant slots.
}
\description{
This function renames a fluorophore across all relevant components of a
\code{Userm} object, including \code{fluors}, matrices, data frames, and result lists.
It ensures consistency of naming throughout the object and updates the
\code{Rename_table} accordingly.
}
\details{
The function performs several checks:
\itemize{
\item Ensures all arguments are provided.
\item Validates that \code{raw_name} exists in \code{Userm$fluors}.
\item Ensures \code{new_name} does not already exist in \code{Userm$fluors}.
\item Updates names in \code{fluors}, \code{A}, \code{Scale_df}, \code{Intensity_mtx}, and \code{Res}.
\item Updates the \code{Rename_table} entry for the fluorophore.
}

If the renaming is successful, a message is printed to the console.
}
\examples{
\dontrun{
# Example usage:
}

}
