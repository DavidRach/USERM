% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Vis_Mtx.R
\name{Vis_Mtx}
\alias{Vis_Mtx}
\title{Visualize a Matrix as a Heatmap with Custom Colors and Labels}
\usage{
Vis_Mtx(
  mat = NULL,
  min = -1,
  mid = 0,
  max = 1,
  mincolor = "blue",
  midcolor = "white",
  maxcolor = "red"
)
}
\arguments{
\item{mat}{A numeric matrix to be visualized. Must not be \code{NULL}.}

\item{min}{Minimum value for the color scale. Default is -1.}

\item{mid}{Midpoint value for the color scale. Default is 0.}

\item{max}{Maximum value for the color scale. Default is 1.}

\item{mincolor}{Color representing the minimum value. Default is \code{"blue"}.}

\item{midcolor}{Color representing the midpoint value. Default is \code{"white"}.}

\item{maxcolor}{Color representing the maximum value. Default is \code{"red"}.}
}
\value{
A \code{Heatmap} object representing the input matrix with customized colors
and cell labels.
}
\description{
This function creates a heatmap visualization of a numeric matrix using the
\code{ComplexHeatmap} package. It allows customization of the color scale and
overlays numeric values on each cell.
}
\details{
The function uses \code{colorRamp2} to define a color gradient and
\code{ComplexHeatmap::Heatmap} to generate the heatmap. Each cell displays its
numeric value formatted to one decimal place. Row and column clustering are disabled.
}
\examples{
\dontrun{
mat <- matrix(runif(25, -1, 1), nrow = 5)
Vis_Mtx(mat)
}

}
