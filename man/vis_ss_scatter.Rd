% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_ss_scatter.R
\name{vis_ss_scatter}
\alias{vis_ss_scatter}
\title{Visualize Spillover Spreading Between Fluorophores}
\usage{
vis_ss_scatter(ss_output)
}
\arguments{
\item{ss_output}{A list returned by \code{\link[=check_ss]{check_ss()}}, containing unmixed matrices \code{B_pos} and \code{B_neg},
fluorophore names \code{f_pos} and \code{f_neg}, and other spread metrics.}
}
\value{
A \code{ggplot} object showing a scatter plot of \code{f_pos} vs \code{f_neg} signal intensities,
with points colored by sample group ("Stain" for positive, "Reference" for negative).
}
\description{
Generates a scatter plot comparing the signal intensities of a positive fluorophore (\code{f_pos})
and a negative fluorophore (\code{f_neg}) across positive and negative control samples.
This visualization helps assess the extent of spillover spreading in spectral flow cytometry.
}
\details{
The function extracts the relevant fluorophore channels from the unmixed matrices,
combines them into a single data frame, and plots the signal distribution using \code{ggplot2}.
This plot provides an intuitive view of how signal from \code{f_pos} may spread into \code{f_neg},
aiding in panel design and spillover diagnostics.
}
\examples{
\dontrun{
ss <- check_ss(f_pos = "FITC", f_neg = "PE", SSM_fluor = c("FITC", "PE"),
               A = A, custom_ssm_dir = "~/custom_ssm")
vis_ss_scatter(ss)
}
}
