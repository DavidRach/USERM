% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Userm_html_table.R
\name{Userm_html_table}
\alias{Userm_html_table}
\title{Generate an HTML table with color-coded cells and a legend}
\usage{
Userm_html_table(
  mat,
  val_min,
  val_mid,
  val_max,
  colormin,
  colormid,
  colormax,
  Legend = "Color Legend"
)
}
\arguments{
\item{mat}{A numeric matrix to be displayed as an HTML table.}

\item{val_min}{Minimum value for color scaling.}

\item{val_mid}{Midpoint value for color scaling.}

\item{val_max}{Maximum value for color scaling.}

\item{colormin}{Color corresponding to \code{val_min}.}

\item{colormid}{Color corresponding to \code{val_mid}.}

\item{colormax}{Color corresponding to \code{val_max}.}

\item{Legend}{A character string for the legend title. Default is \code{"Color Legend"}.}
}
\value{
A character string containing HTML code for the table and legend.
}
\description{
This function creates an HTML representation of a numeric matrix with each cell
color-coded based on its value. A color legend is also appended to help interpret
the color scale.
}
\details{
This function is useful for visualizing matrices with a heatmap-like
color gradient directly in Shiny apps or HTML reports. It relies on a helper
function \code{getColor()} to compute the appropriate background color for each cell.
}
\examples{
mat <- matrix(runif(9, 0, 100), nrow = 3)
rownames(mat) <- paste0("Row", 1:3)
colnames(mat) <- paste0("Col", 1:3)
Userm_html_table(mat, 0, 50, 100, "#0000FF", "#FFFFFF", "#FF0000")

}
